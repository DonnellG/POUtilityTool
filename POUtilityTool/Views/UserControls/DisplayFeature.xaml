<UserControl x:Class="POUtilityTool.Views.UserControls.DisplayFeature"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POUtilityTool.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="510">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="1.5*"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <TextBlock Text="{Binding Id}"               
               Grid.Column="0"/>
    <TextBlock Text="{Binding name}"
               Grid.Column="1"/>
    <TextBlock Text="{Binding userStoryClosedPercentage}"
               Grid.Column="2"/>
    <TextBlock Text="{Binding technicalStoryClosedPercentage}"
               Grid.Column="3"/>
    <TextBlock Text="{Binding spikeClosedPercentage}"
               Grid.Column="4"/>
  </Grid>
</UserControl>
